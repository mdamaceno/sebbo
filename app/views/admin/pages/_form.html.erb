<%= simple_form_for([:admin, @page], html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :title, label: "Título", input_html: { class: "form-control" } %>
  <%= f.input :body, label: "Texto", input_html: { class: "form-control ckeditor" } %> <br/>
  <%= f.button :submit, "Salvar", class: "btn btn-success btn-sm" %>
  <a onclick="history.back(-1)", class="text-danger" style="cursor:pointer">Cancelar</a>
<% end %>
