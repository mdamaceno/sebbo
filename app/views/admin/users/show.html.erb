<div class="panel panel-default">
  <div class="panel-heading"><%= @user.name %></div>
  <div class="panel-body">
    <%= raw @user.body %>
  </div>
</div>
<p><%= link_to "Editar", edit_admin_user_path(@user), class: "btn btn-primary btn-sm" %> <%= link_to "Voltar", admin_users_path(@user), class: "text-danger" %></p>
